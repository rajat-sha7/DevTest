# Settings to manage and configure a DevQnA Prep App Hosting backend.
# https://devqna-prep-docs.example.com/app-hosting/configure

runConfig:
  # Increase this value if you'd like to automatically spin up
  # more instances in response to increased traffic.
  maxInstances: 1
